# ─────────────────────────────────────────────────────────────
# AnswerVault – Environment Variables
# Copy to .env.local for local dev; set in Vercel dashboard for prod
# ─────────────────────────────────────────────────────────────

# ── GitHub OAuth App ──────────────────────────────────────────
# Create at: https://github.com/settings/applications/new
# Homepage URL: https://your-app.vercel.app
# Authorization callback URL: https://your-app.vercel.app/api/auth/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ── Session Security ──────────────────────────────────────────
# Generate with: openssl rand -hex 32
# Either name works — SESSION_SECRET is preferred
SESSION_SECRET=your_random_secret_32_chars_min
# NEXTAUTH_SECRET=  ← also accepted as a fallback

# ── App URL ───────────────────────────────────────────────────
# Local dev: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# ── License System ────────────────────────────────────────────
# Run `npm run setup-keys` to generate your RSA keypair.
# Paste the PUBLIC key value here (entire PEM, newlines as \n).
# The PRIVATE key stays in tools/generate-license/private.pem (gitignored).
# If absent → app runs in DEMO mode.
ANSWERVAULT_PUBLIC_KEY=

# ── License Key (set per-customer in their env) ───────────────
# Customers set this to the token minted with your private key.
# If absent or invalid → DEMO mode.
LICENSE_KEY=

# ── Feature Flags ─────────────────────────────────────────────
# Stale threshold in days (default: 180)
STALE_ANSWER_DAYS=180
STALE_EVIDENCE_DAYS=365

# ── Optional: BYO LLM for smart answer matching ───────────────
# Leave blank to use keyword-based matching (works without any LLM)
OPENAI_API_KEY=
